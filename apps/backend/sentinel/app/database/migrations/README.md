This directory stores baseline SQL migration artifacts.

For now, startup executes `init_db()` to create extension/tables for rapid P2 iteration.
Alembic migration wiring will be added in a later turn once auth and router behavior settle.

