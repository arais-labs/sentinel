[Unit]
Requires=sentinel-apparmor.service
After=sentinel-apparmor.service

[Service]
# Preserve stock Snap mount ordering and lifecycle. Its container allowlist
# skips loading policy in Sentinel's dedicated-VM architecture.
ExecStartPre=/bin/sh /usr/local/libexec/sentinel-apparmor-load snap
